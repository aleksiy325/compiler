struct Two {
    int _a
}

struct Test {
    int _a
    int _b
    int ref _c
    int ref _d
    Two _two
}

def test(Two ref _two) () {
    _two._a = 1
    return
}

# ref _a := 1
ref _two := Two{1}
# _c := Test{0, _a, 0, _a, _two}
test(_two)
print(_two._a) 
